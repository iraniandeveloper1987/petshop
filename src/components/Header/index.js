// With this setup, when you import the Header component in other files, you only need to import the folder (directory), and it will automatically reference the index.js file.
// Example usage in another file:

import Header from "./Header";
export default Header;
